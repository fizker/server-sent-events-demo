{
  "name": "server-sent-events-demo",
  "version": "0.1.0",
  "description": "Demo server and clients for testing Server-Sent Events (EventSource in the browser)",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "ssestream": "^1.0.1",
    "uuid": "^8.3.2"
  },
  "type": "module",
  "private": true,
  "license": "WTFPL"
}
